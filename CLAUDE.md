# mohr-vault

Legacy vault + MCP server setup. The live second brain now lives in the
`dillon-os` repo — treat `vault/00_Memory_File.md` here as historical context,
not current truth.

## Knowledge

- Before starting, read the relevant pages from the `dillon-os` vault —
  `entities/`, `concepts/`, and `01_Clients/` — starting at its `INDEX.md`
  (locally `~/dillon-os`, or checked out alongside this repo).
- Ground every claim about our business, clients, or audience in a vault page,
  and cite the page.
- If the vault isn't available in this session, say so instead of guessing.
