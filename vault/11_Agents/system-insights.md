# System Insights

> Generated by the vault integrity agent. Overwritten each run.
> Last updated: 2026-04-15

## Trends

- **Facebook / Meta Ads is the dominant workstream.** 5 of 6 SOPs, 7 of 8 active campaign queues, and all four "full-strategy" client packages (Jeff Hozias, NKCDC, Florecita, Buzz Bull) center on Meta. Google Ads has one queue file and one agent doc.
- **Session notes are consolidating around Facebook Ads automation.** 4 of 4 active session logs in `10_Sessions/` are Meta Ads API / automation / debug related - suggests a build sprint in progress.
- **Client roster is stable at 12 active accounts.** No new clients added or offboarded since the previous sync.
- **Client substructure is now uniform.** All 12 clients have `Ads/`, `SEO/`, `Reports/` subfolders, and all 12 now carry both `Agent Memory.md` and `Reporting Log.md` at the root (Buzz Bull and Florecita were backfilled this run).

## Gaps

- **SEO coverage is thin.** Only one SEO asset in the vault (`03_Content/SEO Keyword Targets.md`) and one agent doc (`11_Agents/SEO Agent.md`). No per-client rank tracking, no keyword maps, no technical audit logs. Every per-client `SEO/` subfolder is still empty.
- **Reporting is logged but not aggregated.** Each client has a `Reporting Log.md`, but there is no cross-client rollup or monthly summary anywhere in the vault.
- **Google Ads SOPs are missing.** All 5 SOPs in `04_SOPs/` are Facebook-specific. Google Ads work has no documented playbook.
- **Daily notes folder is empty.** `07_Daily_Notes/` has no entries - either the daily note habit has lapsed or entries are being written elsewhere.
- **Inbox triage is current.** `00_Inbox/` is empty - good, nothing backlogged.
- **Client strategy file placement is mixed.** Jeff Hozias, NKCDC, Florecita, and Buzz Bull keep `Creative Angles.md`, `Facebook Ads Strategy.md`, `Facebook Ads Testing Roadmap.md`, and `Facebook Ads Account Notes.md` at the client root, while other clients keep equivalent notes inside `Agent Memory.md`. Convention has not been chosen.

## Opportunities

1. **Stand up per-client SEO tracking.** Use each `ClientName/SEO/` folder to hold monthly rank snapshots, keyword targets, and GSC exports. Start with the four full-strategy clients.
2. **Create a monthly reporting rollup** at `11_Agents/` or `10_Sessions/` (e.g. `11_Agents/2026-04-rollup.md`) that aggregates performance deltas across all 12 clients.
3. **Document Google Ads SOPs.** At minimum: Launch, Weekly Optimization, Search Terms Review. The infrastructure (queue files, agent doc) already assumes they exist.
4. **Pick one home for client strategy notes.** Either fold the loose "Facebook Ads *.md" files into each client's `Agent Memory.md`, or adopt a per-client `Strategy/` subfolder and migrate all clients to match.
5. **Archive the Facebook Ads build sprint** once shipped. The `10_Sessions/` logs should graduate to a completed-sprint note in `11_Agents/` or an SOP so the knowledge persists past the build window.
6. **Revive daily notes** using `_templates/daily-note.md` - even a 30-second end-of-day entry into `07_Daily_Notes/` gives future syncs a signal for weekly review generation.

## Structural notes

- The prior run created unnumbered `/vault/SEO/` and `/vault/Reports/` folders that duplicated the canonical numbered scheme. This run consolidated them: `system-insights.md` was moved (via `git mv`) to `/vault/11_Agents/system-insights.md`, and both empty duplicate folders were removed. Going forward the numbered folders (`00_Inbox` through `11_Agents`, plus `_templates`) are the single source of truth.

---
